---
description: AWS EC2 闲置检测工具的开发规范。修改 EC2 相关函数、工具注册、检测逻辑时自动生效。
globs: cloud_monitor/tools/aws.py,cloud_monitor/agent.py,cloud_monitor/config.py,main.py
alwaysApply: false
---

# AWS EC2 闲置实例检测 — 开发规范

## 1. 架构总览

```
config.yaml (ec2_check)
    ↓
config.py (EC2CheckConfig)
    ↓
┌───────────────────────────┐    ┌──────────────────────────┐
│ agent.py                  │    │ main.py --ec2            │
│ aws_ec2 工具注册          │    │ direct_ec2_check()       │
│ (默认值从 EC2CheckConfig) │    │ (默认值从 EC2CheckConfig)│
└──────────┬────────────────┘    └──────────┬───────────────┘
           ↓                                ↓
     aws.py: list_ec2_aws()  ← 统一入口，带缓存
           ↓
     aws.py: _run_ec2_scan() ← 实际扫描逻辑
           ↓
     aws.py: _check_instance_utilization() ← 单实例 CloudWatch 查询
```

工具名称：`aws_ec2`（函数 `list_ec2_aws`），一次调用返回全部结果：

| 输出模块 | 格式 | 说明 |
|---------|------|------|
| 实例概览 | 纯文本 | 按区域统计运行/停止/总数 |
| 已停止实例 | 多行详情（InstanceInfo.display） | ID、名称、类型、区域、可用区、IP、停止时长、EBS、安全组、标签 |
| 低利用率实例 | **紧凑 Markdown 表格** | 一行一条：实例ID / 名称 / 类型 / 区域 / 私有IP / 平均CPU / 最高CPU / 平均内存 / 最高内存 / 标签 |

## 2. 配置规范

```yaml
# config.yaml
ec2_check:
  cpu_threshold: 10.0   # CPU 利用率阈值 %
  mem_threshold: 10.0   # 内存利用率阈值 %（需安装 CloudWatch Agent）
  hours: 360            # 检测时间窗口（小时，360 = 15天）
  max_workers: 20       # CloudWatch API 并发线程数
```

- Agent 模式调用 `aws_ec2` 时**不传参数**，默认值从 `EC2CheckConfig` 注入
- CLI 模式 `--cpu / --mem / --hours` 仅做覆盖，不传则用配置值

## 3. 检测逻辑

| 类别 | 数据源 | Namespace | MetricName | Period | 判定条件 |
|------|--------|-----------|------------|--------|---------|
| 已停止 | EC2 API | - | - | - | `state == "stopped"` |
| CPU 闲置 | CloudWatch | `AWS/EC2` | `CPUUtilization` | 3600s | avg < `cpu_threshold` |
| 内存闲置 | CloudWatch | `CWAgent` | `mem_used_percent` | 3600s | avg < `mem_threshold` |

- CPU 或内存**任一**低于阈值即标记为低利用率
- 未安装 CloudWatch Agent 的实例，内存查询静默跳过（不报错、不标记）

## 4. 性能优化

- **两阶段执行**：先通过 EC2 API 收集所有实例列表，再并发查 CloudWatch
- **并发查询**：`ThreadPoolExecutor(max_workers)` 同时查询 CPU + 内存
- **进度输出**：每 50 个实例向 stderr 打印一次进度
- **结果缓存**：`_ec2_scan_cache` 以 `account|region|cpu|mem|hours` 为 key 缓存完整结果，后续调用瞬间返回

## 5. 关键约束

1. **中文输出** — 所有字段名、状态描述必须中文
2. **异常隔离** — 单实例 CloudWatch 查询失败不影响整体报告
3. **时间自适应** — >= 48h 显示"X天"，< 48h 显示"X小时"
4. **区域不重复** — `InstanceInfo.region` 已输出区域，`extra` 中禁止再加 `区域` 字段
5. **不输出正常实例** — 运行中且利用率正常的仅计入概览数字
6. **低利用率用表格** — 禁止用多行 InstanceInfo.display()，必须用紧凑 Markdown 表格
